# Story 8.5: Frontend — Sharing & Analytics API helpers

Status: done

Minimal scope to match 7.6 pattern — API helpers + pragmatic hooks. Full mock-replacement across the UI deferred to future UX tuning epic (the existing mocks keep demo flows working).

## AC

1. `features/shares/api.ts` — `listShares`, `createShare`, `revokeShare`.
2. `features/analytics/api.ts` — `getAnalytics`.
3. Typecheck/lint/build green.

## File List
